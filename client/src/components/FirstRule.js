import React from 'react';

const FirstRule = () => {
    return(
        <p>If the first two cards of either side equal to 8 or 9, we call this a Natural and the game is over. Whichever side holds the higher value wins (or ties). If there is no "Natural", proceed to step 2</p>
    )
}

export default FirstRule;